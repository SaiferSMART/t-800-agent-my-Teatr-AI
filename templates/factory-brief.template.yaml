# Бриф конвейера — flat YAML; сохранять в {memory_path}/factory-briefs/<slug>.yaml
slug: <slug-прогона>
version: "..."
date: "YYYY-MM-DD"
no_new_research_brain: true
optional_new_agents: []
plugin_root: "<plugin_root из discovery>"
memory_path: "<memory_path из discovery>"

goal: >
  ...

must_create:
  - ...

must_update:
  - ...

contracts_sketch:

  <contract-key>:
    ...

constraints:
  - ...

smoke_after_install:
  - ...
