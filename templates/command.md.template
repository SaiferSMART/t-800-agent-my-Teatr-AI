# {{COMMAND_TITLE}}

> Minimal stub. Реальные команды — оркестрации конвейера, см. `commands/t800-start.md`, `commands/t800-fix.md`.

**Обязательно** вызови субагента через `Task({{AGENT_NAME}})`.

Передай контекст:
- цель пользователя;
- имя/идея нового агента (если есть);
- категория плагина;
- что уже создано.

Если `Task({{AGENT_NAME}})` недоступен:

```
Task(generalPurpose)
```

Промпт: полное содержимое `agents/{{AGENT_NAME}}.md` из плагина.
