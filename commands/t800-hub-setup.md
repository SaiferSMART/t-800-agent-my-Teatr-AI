# /t800-hub-setup — алиас

Команда-алиас. Выполни:

```
/t800-cloud-hub
```

Тот же лид (`t-800-cloud-hub-lead`), тот же контракт (`shared/cloud-hub-setup-contract.md`).

Содержимое идентично `commands/t800-cloud-hub.md`.
